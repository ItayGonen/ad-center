# demo.leadsadv.com — HTTP → HTTPS redirect
server {
    listen 80;
    server_name demo.leadsadv.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# demo.leadsadv.com — HTTPS
server {
    listen 443 ssl;
    server_name demo.leadsadv.com;

    ssl_certificate     /etc/letsencrypt/live/demo.leadsadv.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/demo.leadsadv.com/privkey.pem;

    # API → backend-demo
    location /api/ {
        proxy_pass http://backend-demo:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Uploads served from backend
    location /uploads/ {
        proxy_pass http://backend-demo:8000/uploads/;
        proxy_set_header Host $host;
    }

    # Frontend SPA
    location / {
        proxy_pass http://frontend-demo:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
